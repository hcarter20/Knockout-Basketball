using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
/* commented out to preserve the rest of the game atm
public class Meter : MonoBehaviour
{
    [SerializeField] private Image marker;

    [SerializeField] public Player;
    public float trol = Player.PlayerControl; 
    public float throwPower = trol.throwSpeed;
    // this is supposed to access the throw speed variable T-T */ 
    // Update is called once per frame
    /*void Update()
    {
        
    }

    void setMarker()
    {
        marker.transform.localEulerAngles = new Vector3(0,0,(-32 + (throwPower - 5)*(64)/(5)));
    }
} */
